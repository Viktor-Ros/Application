<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/linearLayout2"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000"
    android:orientation="vertical"
>

    <ScrollView
        android:id="@+id/SCROLLER1"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:backgroundTint="#3C3F41"
        android:fillViewport="true"
        android:scrollbars="vertical">

        <!-- почему у LinterLayout высота - match_parent? Почему ну match_parent-->
        <LinearLayout
            android:id="@+id/linearLayout1"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="#000000"
            android:orientation="vertical"
            android:paddingLeft="5dp"
            android:paddingRight="5dp">

<!--            Ты же потом создашь ещё таких кнопок. Есть какой-то способ вынести её в отдельную xml?-->
            <Button
                android:id="@+id/button"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Новая трапеза" />

        </LinearLayout>
    </ScrollView>
</LinearLayout>